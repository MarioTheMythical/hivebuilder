import React from 'react';
import ReactDOM from 'react-dom/client';
import HiveBuilder from './Pages/HiveBuilder';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HiveBuilder />
  </React.StrictMode>
);